"""Validation gates that operate directly on a user's question folder.

Each gate remains independently executable, depends only on the standard
library, and includes a self-test proving that its negative controls go red.
"""
